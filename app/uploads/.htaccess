# Prevent PHP execution in uploads directory
<FilesMatch "\.php$">
    deny from all
</FilesMatch>

# Only allow image files
<FilesMatch "\.(jpg|jpeg|png|webp|gif)$">
    allow from all
</FilesMatch>
